# code-generator
搭建出码

```
import codeGenerator from '@mybricks/code-generator'

// target = 'react' | 'weapp'
const codeResult = codeGenerator.generateCode({ toJson: json, target: 'react' })

console.log(codeResult)

```